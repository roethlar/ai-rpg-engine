[package]
name = "aetheria-desktop"
version = "0.1.0"
description = "Standalone desktop shell for the Aetheria GM dev server"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
