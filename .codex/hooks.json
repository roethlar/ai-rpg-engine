{
  "hooks": {
    "SessionStart": [
      { "matcher": "compact",
        "hooks": [ { "type": "command", "command": "echo 'Context was compacted or the session restarted. Before continuing, re-read AGENTS.md from disk, especially the Prime Invariants block. Treat AGENTS.md, not this message, as authoritative.'" } ] }
    ],
    "PreToolUse": [
      { "matcher": "apply_patch|Edit|Write",
        "hooks": [ { "type": "command", "command": "py -3 \"$(git rev-parse --show-toplevel)/.codex/agents-md-tripwire.py\" 2>/dev/null || python3 \"$(git rev-parse --show-toplevel)/.codex/agents-md-tripwire.py\"" } ] }
    ]
  }
}
