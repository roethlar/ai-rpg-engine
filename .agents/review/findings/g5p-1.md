# g5p-1: Carry pt-5 description scope into the replacement gate order

**Severity**: MEDIUM — a cold implementer could ship S1.5 with the same owner-unruled
campaign-tailored-description lifecycle already admitted as `pt-5`.

**Status**: Open

**Branch**: `master` (docs-only plan repair; one finding per commit)

**Commit**: pending

## Evidence

At reviewed SHA `d00c34f77cd9e0c6a72345e45874292aa941f831`,
`.agents/review/gate-5-class-model-plan.md:289` lets Creator write a description around the approved
build, while §8 and §9 omit the separate `pt-5` ruling. `.agents/review/findings/pt-5.md` records that
the description is campaign-tailored but frozen across moves without a settled destination surface.

## Predicted observable failure

G5-D is treated as the complete S1.5 gate and implements an origin-campaign description such as
“netrunner” as stable identity or destination narration authority in a fantasy campaign, despite no
owner ruling on that lifecycle.

## What

The replacement taxonomy fixes class/role conflation but does not close or carry forward the
independent description-scope blocker.

## Approach

Name `pt-5` in §7, require its owner ruling before G5-D, and add its finding/state/plan records to
the eventual docs-update checklist. Do not choose the description lifecycle in this repair.

## Files changed

- `.agents/review/gate-5-class-model-plan.md` — pending

## Guard proof

Docs-only: `git diff --check`; verify the plan names `pt-5` in the Creator flow, gate order, and
durable-update checklist.

## Coder dispute (if any)

None.

## Known gaps

The owner still must rule the actual description lifecycle separately.

## Reviewer comments

Reviewer: claude / claude-fable-5 / high / standard (inline, session-only)

Claude CLI 2.1.220; reviewed `d00c34f77cd9e0c6a72345e45874292aa941f831` against
`03739475c8e7ec1fb72e0884bbd8b88b72d5733e`; `guard_confirmed: true`;
`capability_ok: true`; verdict `reopened`; result UUID
`13a849c6-1587-4873-9edc-40e0511eabcd`; session
`565def47-5355-43c1-9231-c92cc9410e66`.

Comment: the plan reintroduces Creator-written description without naming `pt-5`'s unresolved
storage and post-move surface, so the gate sequence is falsely complete.
