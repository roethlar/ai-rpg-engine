Transport note: the previous r4 dispatch aborted at the transport layer; you may or may not have completed it. If your prior turn already produced the full r4 envelope against pin 4d9d12e6792f084100d421fb0a8e4302a89d0557, re-emit that JSON envelope now, verbatim. Otherwise perform the review now as specified below. This message is the authoritative round r4 dispatch.

Round r4 repair-delta review. Draft r4 of docs/runtime-greenfield-plan.md is on disk. Pinned git blob SHA: 4d9d12e6792f084100d421fb0a8e4302a89d0557 (519 lines, 8 top-level sections). Re-read the FULL file from disk first; run `git hash-object docs/runtime-greenfield-plan.md` and confirm it matches the pin before reviewing. If it does not match, stop and return verdict "stale_pin".

All 11 r3 findings were accepted and repaired in draft r4:
- rg-r3-1 (HIGH, HMAC key reveal): §3 dice verification split into two properties — live per-turn (commitment column fixed pre-model-call, verified by recompute at reveal) vs at-root-reveal (seeds honestly derived from committed root); root revealed at campaign end or key rotation; verification vectors updated.
- rg-r3-2 (HIGH, POST /turn idempotency): exchange record persisted before any generation dispatch, unique over (campaign, seat, request_id) with canonical request hash; duplicate replays stored result to that seat only (audience-scoped); same key + different hash rejects. §2 and §7.
- rg-r3-3 (HIGH, mutation-channel bypass): fact_ops / npc_updates arrays removed; single canonical mutation channel via op v0 with `record_fact` op and slot-based conflict keys.
- rg-r3-7 (HIGH, ACL starvation): dual-projection output — requester-private segments plus private-answer fixtures; §5 audience rules.
- rg-r3-10 (HIGH, DDL rollback): min_runtime_version fence, one-way activation, backup-restore rollback; G3 moved before any migration code.
- rg-r3-4: stage-enum generation caps (3/4/5) reconciled with continuation rules.
- rg-r3-5: path matrix now computed→padded columns with labeled multipliers.
- rg-r3-6: validation re-prompt vs escalated retry rates split, per-scenario lines (E $22.81, A ≈ $66.4).
- rg-r3-8: gate G4 pins router feature names.
- rg-r3-9: TOST-style non-inferiority spec with derived n (SD 1.25 → 18 → ≥25).
- rg-r3-11: gate G5 seeded soak manifest; §6.5 exchange-status denominator with per-status thresholds.

Your task, same adversarial standard as r1–r3 (this plan gates a real rebuild; a missed defect costs far more than a false alarm):
1. For each r3 finding, verify the repair in the actual r4 text: status one of "repaired", "partial", "regressed", with a one-line note citing the section.
2. Fresh full-document pass — changed AND unchanged text. Re-derive all arithmetic yourself (path matrix, scenario lines, envelope totals, soak thresholds, TOST n). Hunt for: new contradictions introduced by the repairs, cross-section drift, undefined terms used normatively, unimplementable specs, cost-envelope violations vs the owner constraint (campaign ≤ $50 all-in, AAA-comparable $/hour), security/abuse gaps, and anything a competent implementer would have to guess.
3. Findings get ids rg-r4-N. severity HIGH/MEDIUM/LOW; include exact quote, failure_scenario, suggested_fix, section.

Return ONLY one JSON object, no prose outside it:
{"reviewed_sha": "...", "evidence_checked": true, "arithmetic_rechecked": true, "repairs": [{"id": "rg-r3-1", "status": "repaired|partial|regressed", "note": "..."}], "findings": [{"id": "rg-r4-1", "severity": "HIGH|MEDIUM|LOW", "section": "...", "quote": "...", "failure_scenario": "...", "suggested_fix": "..."}], "verdict": "approved|reopened|stale_pin"}
Verdict "approved" only if zero HIGH and zero MEDIUM findings and all 11 repairs are "repaired".
