# handoff

Read the "Operator Requests" section of `AGENTS.md` and follow the `handoff` process: update `.agents/state.md` (Now/Next/Blockers/Verification/Unrecorded) so the next session can resume cleanly without relying on chat context. Keep it short and factual.