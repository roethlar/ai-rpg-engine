# drift

Read the "Operator Requests" and "Source Of Truth" sections of `AGENTS.md` and follow the `drift` process: compare the target doc/decision/guidance claim against current repo evidence (code, tests, package.json, plan.md, README.md, git state); fix the lower-authority source or report the unresolved conflict. Guidance files (`AGENTS.md`, `.agents/*`) are valid drift targets.