# decision

Read the "Operator Requests" section of `AGENTS.md` and follow the `decision` process: record a settled durable decision in `.agents/decisions.md` (with Status, Decision, Reason, Supersedes) and update any affected guidance (AGENTS.md or state). Use generalized wording that stands without chat history.